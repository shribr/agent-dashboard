{
  "name": "agent-dashboard-relay",
  "version": "1.0.0",
  "description": "Cloudflare Worker relay for Agent Dashboard iOS app",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.63.0"
  }
}
